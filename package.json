{
  "name": "hogwarts-houses-app",
  "version": "1.0.0",
  "description": "Simple demo application with React v19 (Typescript) frontend and Node.js backend",
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm run start",
    "start:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "cleanup": "rm -rf node_modules && cd backend && rm -rf node_modules && cd ../frontend && rm -rf node_modules"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
